---
github: https://github.com/LunaDeerMC/ExpExtraction
license:
  name: MIT
  url: https://github.com/LunaDeerMC/ExpExtraction/blob/master/LICENSE
links:
  - text:
      zh-CN: 源码
      en: Source
    icon: Code2
    url: https://github.com/LunaDeerMC/ExpExtraction
  - text:
      zh-CN: 问题反馈
      en: Issues
    icon: CircleAlert
    url: https://github.com/LunaDeerMC/ExpExtraction/issues
  - text:
      zh-CN: 获取支持
      en: Support
    icon: Heart
    url: /support#community
acquisition:
  - id: open-source
    name:
      zh-CN: 开源构建版
      en: Open-source build
    access: free
    description:
      zh-CN: 从 GitHub 获取发行文件；如果当前没有适合你的发行文件，可以按安装文档从源码构建。
      en: Get a release file from GitHub, or build the plugin from source when no suitable release is available.
    benefits:
      - zh-CN: MIT License 授权
        en: MIT License
      - zh-CN: 支持 Paper 与 Folia
        en: Supports Paper and Folia
      - zh-CN: 命令和附魔台两种经验提取方式
        en: Command and enchanting-table extraction
      - zh-CN: 轻量的单用途经验存储功能
        en: Lightweight, single-purpose experience storage
    channels:
      - name: GitHub Releases
        url: https://github.com/LunaDeerMC/ExpExtraction/releases
        actionLabel:
          zh-CN: 查看发行文件
          en: View releases
      - name: GitHub Repository
        url: https://github.com/LunaDeerMC/ExpExtraction
        actionLabel:
          zh-CN: 查看源码
          en: View source
---

import Localized from "../../../components/shared/Localized.astro";

<Localized locale="zh-CN">

## 作品介绍

ExpExtraction 是一个轻量的 Minecraft 经验存储插件。它把玩家当前的一部分经验封装进带有自定义数据的附魔之瓶，玩家可以把经验暂时保存起来，也可以把瓶子交给其他玩家使用。

当前介绍以仓库 `master` 分支和 `1.3.2` 构建配置为基准。插件声明使用 Minecraft `1.20` API，并支持 Folia；实际部署时仍应先在目标服务端和测试世界中验证。

### 当前能力

- 使用 `/extract [amount]` 将指定数量的经验提取到经验瓶；省略数量时使用玩家当前总经验，但单瓶仍受命令提取上限限制。
- 潜行时手持空玻璃瓶右键附魔台，按配置提取固定数量的经验。
- 手持由插件生成的经验瓶右键使用，经验会直接返还给使用者，瓶子随后消耗一个。
- 经验瓶带有经验数量和来源玩家信息，并使用自定义数据与普通经验瓶区分。
- 支持 Paper 与 Folia，插件本身不需要数据库、外部服务或额外依赖。

### 使用前需要知道

ExpExtraction 只负责封存和取回经验，不改变 Minecraft 的等级计算方式，也不会把普通的原版经验瓶转换成可存储经验瓶。当前默认命令单瓶上限为 `2000` 点，附魔台每次默认提取 `100` 点。

当前源码中 `enchantingTable.enable` 虽然会写入配置文件，但附魔台事件实际检查的是 `commandExtraction.enable`。因此，如果需要关闭创建经验瓶的功能，应将 `commandExtraction.enable` 设为 `false`；已经存在的经验瓶仍然可以被使用。

### 文档入口

- [作品介绍](/docs/plugins/exp-extraction)：了解功能、适用范围和当前行为基准。
- [使用说明](/docs/plugins/exp-extraction/usage)：面向普通玩家，说明命令、附魔台操作和经验瓶使用方式。
- [安装与配置](/docs/plugins/exp-extraction/installation)：面向管理员，说明安装、权限、配置和升级检查。

</Localized>

<Localized locale="en">

## Introduction

ExpExtraction is a lightweight Minecraft experience-storage plugin. It packages part of a player's current experience into an enchanted bottle with custom data, allowing the experience to be saved for later or handed to another player.

This page uses the repository's `master` branch and `1.3.2` build configuration as its reference. The plugin declares the Minecraft `1.20` API and Folia support; always verify it on the target server and a test world before a production deployment.

### Current capabilities

- Use `/extract [amount]` to put a chosen amount of experience into a bottle. Without an amount, the command uses the player's total experience, still subject to the per-bottle command limit.
- Sneak while holding an empty glass bottle and right-click an enchanting table to extract the configured amount.
- Right-click while holding a plugin-created experience bottle to return the stored experience to the user; one bottle is consumed.
- Add the stored amount and source player to the bottle and distinguish it from an ordinary experience bottle with custom data.
- Support Paper and Folia without a database, external service, or additional runtime dependency.

### Before using it

ExpExtraction only stores and returns experience. It does not change Minecraft's level calculation and does not convert ordinary vanilla experience bottles into storage bottles. The default command limit is `2000` points per bottle, while the enchanting-table method extracts `100` points per use by default.

In the current source, `enchantingTable.enable` is written to the configuration file, but the enchanting-table event checks `commandExtraction.enable` instead. To disable bottle creation, set `commandExtraction.enable` to `false`; existing bottles can still be used.

### Documentation entry points

- [Introduction](/docs/plugins/exp-extraction): scope, features, and the current behavior baseline.
- [Usage](/docs/plugins/exp-extraction/usage): commands, enchanting-table extraction, and using a stored-experience bottle.
- [Installation and configuration](/docs/plugins/exp-extraction/installation): installation, permissions, configuration, and upgrade checks.

</Localized>
