---
title: DeerFoliaPlus · Troubleshooting
description: Diagnose DeerFoliaPlus startup, configuration, client-protocol, fake-player, custom-recipe, and Folia threading problems.
icon: CircleHelp
sidebar:
  label: Troubleshooting
  order: 40
---

## Order of operations

Stop the server and back up the current directory first. Record the DeerFoliaPlus tag, Minecraft version, Java version, launch arguments, plugin list, client mods, and full log. Change one variable at a time; do not start by deleting worlds, config, or fake-player data.

## The core will not start

Check:

1. whether the actual process uses Java 25;
2. whether the launch command loads only one core jar;
3. whether applyAllPatches or the source build completed;
4. whether the server user can read and write the core, config, and world directories;
5. the first ERROR in the log, rather than the final cascade of errors.

For a configuration parsing error, move config/deer-folia-plus.yml to a recoverable backup location, let the core generate a fresh file, and migrate old values group by group. Do not rename a DeerFolia config into the DeerFoliaPlus config.

## Configuration has no effect

deer-folia-plus.yml is loaded at startup, so fully restart after editing. Check kebab-case keys such as resident-bot, amount-per-player, and skip-fabric-on-via-non-native. Do not accidentally turn true, false, numbers, or lists into unexpected strings.

custom-recipes.yml registers recipes only when custom-recipe.enabled is true. Check parse errors, unknown item IDs, recipe type, and pattern before testing the relevant container.

## Client-protocol problems

When only some players cannot see recipes, schematics, overlays, or entity data:

- confirm whether the client is Fabric or NeoForge;
- confirm that the client mod matches Minecraft 26.2;
- check the server's recipe-sync, syncmatica, or servux switch;
- keep skip-fabric-on-via-non-native enabled for ViaVersion/ViaBackwards connections while testing;
- disable one protocol group at a time and reconnect instead of turning everything off at once.

Enabling a server protocol does not install a client mod for players.

## FakePlayer problems

When a bot cannot be created, check fake-player.enable, bukkit.command.bot, name length, name collisions, and amount-per-player. A bot.amount.N permission overrides the default limit for a non-OP player.

When a bot does not return after restart, confirm resident-bot is true and that fakeplayer.dat and fakeplayerdata/ under the relevant world are readable and writable. This is world-save data; back it up separately from config.

For action or inventory problems, stop the action, check the region location and client view, and reduce bot count before changing more settings. Do not use reload as a substitute for restart.

## Posture and chairs

Confirm posture.enabled, chair-interaction, and the relevant permission node. A locked sitting or lying position is intentional; use sneak, jump, or /get-up to recover. For chair detection, check contiguous stairs/slabs, their orientation, and require-side-signs.

## Custom recipes

Check each item:

- the top-level key is recipes;
- every recipe ID is unique;
- type is shaped, shapeless, smelting, blasting, smoking, campfire_cooking, or stonecutting;
- ingredient, pattern, ingredients, and result match the selected type;
- item IDs use a valid namespace;
- enchantments, custom-id, and nbt use supported values;
- the restart log reports the expected registration count.

Start with one minimal recipe, then add names, enchantments, and NBT. For recipe-overlap problems, remove or rename the test recipe instead of experimenting in the production world.

## Folia compatibility

DeerFoliaPlus still uses a regionized threading model. Plugins or scripts that force entity, world, or Bukkit API calls onto a traditional single main thread can produce thread-context errors or incorrect behavior. Reproduce without the suspected plugin in a test directory before contacting its author or opening a DeerFoliaPlus issue.
